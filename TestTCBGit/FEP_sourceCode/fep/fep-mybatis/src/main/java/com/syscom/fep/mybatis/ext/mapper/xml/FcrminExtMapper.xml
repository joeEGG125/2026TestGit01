<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.syscom.fep.mybatis.mapper.FcrminExtMapper">

  <select id="checkRemitNumberExist" parameterType="map" resultType="int">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    select
    Count(*)
    from FCRMIN
    where FCRMIN_TXDATE = #{txDate,jdbcType=CHAR}
      and FCRMIN_SENDER_BANK = #{snederBank,jdbcType=CHAR}
      and FCRMIN_RECEIVER_BANK = #{receiverBank,jdbcType=CHAR}
  </select>
</mapper>